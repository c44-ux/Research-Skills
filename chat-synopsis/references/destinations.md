# Destinations

Configured defaults for where chat synopses are saved. The agent reads this file on each run (Step 2).

## Configured Defaults

<!-- Edit after first-run wizard, or by hand. Leave placeholders until configured. -->

| Field | Value |
|-------|--------|
| **destination_type** | _(not set)_ |
| **path_or_id** | _(not set)_ |

**Examples after setup:**

```markdown
| destination_type | obsidian |
| path_or_id | C:\Users\you\Documents\Obsidian\MyVault\Synopses |
```

```markdown
| destination_type | project |
| path_or_id | .cursor/chat-synopses |
```

## Per-destination notes

- **obsidian** — Must be an absolute path to a folder that already exists (or the user confirms creating it).
- **local** — Default root: `~/chat-synopses/` (Windows: `%USERPROFILE%\chat-synopses\`).
- **project** — Relative to workspace root; good for team repos.
- **notion** / **drive** — Requires the corresponding MCP server; fall back to file or chat if missing.
- **chat_only** — No file write; full markdown in the assistant message only.

## Per-run overrides

Triggers like `synopsis this chat → clipboard` or `→ project` override defaults for one run only. Do not edit this file for overrides.
