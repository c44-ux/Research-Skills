# Destinations

Configured defaults for where method synopses are saved. The agent reads this file on each run (Step 2).

## Configured Defaults

<!-- Edit after first-run wizard, or by hand. Leave placeholders until configured. -->

| Field | Value |
|-------|--------|
| **destination_type** | _(not set)_ |
| **path_or_id** | _(not set)_ |
| **method_playbook_folder** | _(not set — e.g. `.cursor/method-playbooks`) |

**Examples after setup:**

```markdown
| destination_type | obsidian |
| path_or_id | C:\Users\you\Documents\Obsidian\MyVault\Method-Synopses |
| method_playbook_folder | C:\Users\you\Documents\Obsidian\MyVault\Method-Playbooks |
```

```markdown
| destination_type | project |
| path_or_id | .cursor/method-synopses |
| method_playbook_folder | .cursor/method-playbooks |
```

## Per-destination notes

- **obsidian** — Must be an absolute path to a folder that already exists (or the user confirms creating it).
- **local** — Default root: `~/method-synopses/` (Windows: `%USERPROFILE%\method-synopses\`).
- **project** — Relative to workspace root; good for team repos.
- **method_playbook_folder** — Where **method-synopsis** reproducibility mode saves rerunnable study docs. See [method-playbook.md](method-playbook.md).
- **notion** / **drive** — Requires the corresponding MCP server; fall back to file or chat if missing.
- **chat_only** — No file write; full markdown in the assistant message only.

## Per-run overrides

Triggers like `method synopsis → clipboard`, `method synopsis this research → project`, or `→ method-playbooks` override defaults for one run only. Do not edit this file for overrides.
